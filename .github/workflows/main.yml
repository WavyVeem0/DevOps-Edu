name: CI

on:
  workflow_dispatch:
  push:
    branches:
      – main
  pull_request:
    branches:
      – main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: "Run Docker Compose"
      run: docker-compose up -d
