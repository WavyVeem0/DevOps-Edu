name: CI

on:
  push:
    branches:
      – main
  pull_request:
    branches:
      – main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: "Run Docker Compose"
      run: sudo docker compose up --build
